# 스터디별 설정 — Notes on Kalman Filter (KF, EKF, ESKF, IEKF, IESKF) / Gyubeom Edward Im

# ── PDF ──────────────────────────────────────────────────────────────
pdf = ref/Notes on Kalman Filter(KF, EKF, ESKF, IEKF, IESKF).pdf

# ── 페이지 오프셋 ────────────────────────────────────────────────────
# PDF 페이지 = 책에 인쇄된 쪽번호 + page_offset
# 검증: PDF p2/p25/p49 푸터가 각각 "STUDY NOTE 2 ...", "... 25 ...", "... 49 ..." → 오프셋 0.
page_offset = 0

# ── 학습 범위 ────────────────────────────────────────────────────────
last_chapter = 13
book_pages = 1-49

# ── 캡션 형식 ────────────────────────────────────────────────────────
# ⚠️ 이 문서에도 Figure/Table 캡션이 하나도 없다 (전문 스캔 0건).
#    그림 24개는 tools/extract_images_bbox.py 가 get_image_info() 의 bbox 를 clip 으로
#    직접 렌더링해 뽑는다. 아래 두 값은 이 스터디에서 사용되지 않는다.
caption_style = flat
caption_bold  = no

# ── 그림 추출 레이아웃 (pt 단위) ─────────────────────────────────────
# 판형: Letter 612×792. 실측(PDF p6): 본문 컬럼 x 90.0–513.0, 푸터 y 773.0
clip_x   = 85-518
header_y = 100
body_x   = 95-508

# ── 메모 ─────────────────────────────────────────────────────────────
# 그림 위치(PDF 쪽): 3 4 5 6(4개) 7 10 13(2개) 15 18 23 26 32 38 45(2개) 46(2개) 47(2개) 48
#                    — 합계 24개
# 번호 붙은 수식: (1)~(166)

# ── 사이드바 머리말 (build_html.py) ──────────────────────────────────
brand     = Notes on Kalman Filter
booktitle = Gyubeom Edward Im — 스터디 노트
