# 스터디별 설정 — Errors and Jacobian Derivations for SLAM
#                   Gyubeom Edward Im

# ── PDF ──────────────────────────────────────────────────────────────
pdf = ref/Errors and Jacobian Derivations for SLAM.pdf

# ── 페이지 오프셋 ────────────────────────────────────────────────────
# PDF 페이지 = 인쇄 쪽번호 + page_offset
# p7/p22 푸터가 "STUDY NOTE 7/22 STUDY NOTE" → 오프셋 0
page_offset = 0

# ── 학습 범위 ────────────────────────────────────────────────────────
# 1~10장 (9 References, 10 Revision log 포함)
last_chapter = 10
book_pages = 1-45

# ── 캡션 형식 ────────────────────────────────────────────────────────
# 그림 캡션이 하나도 없다. 전문에서 Figure/Fig./Table 이 모두 0회.
#   → extract_figures.py 를 쓸 수 없다. tools/extract_images_bbox.py 를 쓴다.
caption_style = flat
caption_bold  = no

# ── 그림 추출 레이아웃 (pt 단위) ─────────────────────────────────────
# 판형: A4 595.28×841.89
clip_x   = 52-566
header_y = 52
body_x   = 60-556

# ── 메모 ─────────────────────────────────────────────────────────────
# 지금까지 일곱 스터디 중 가장 크다 — 45쪽, 식 (1)~(204), 절 42개.
#
# 번호 붙은 수식: (1)~(204) — 누락·중복 없음, 순서 일치
#
# 그림 9개 = 이미지 객체 9개 (1:1). bbox 가 전부 페이지 안쪽이다.
#   p7(2) p14(2) p22 p27 p28 p31 p32
#   p7·p14 의 두 번째 그림은 가로로 긴 "투영 파이프라인 표" 이미지다 (454×55, 454×60).
#
# ★ 색은 한 가지뿐이다 — #197fb2 (산문 강조), 2382 span · 27쪽.
#   수식 안에는 색이 없다. 그래서 이 스터디에서는 \color 를 쓰지 않고
#   ==강조== → <span class="hl"> 만으로 끝난다.
#   dump_colored.py 의 NAME 목록도 하나로 줄여 두었다.
#
# "Code implementations" 절(3.3 · 4.2 · 5.2 · 6.5 · 7.3 · 8.1.1 · 8.2.1 · 8.3.3)은
#   전부 g2o/GTSAM 소스 링크 목록이다. 코드 리스팅이 박혀 있지 않다
#   (문서 전체에 고정폭 폰트가 0회 쓰였다).
#
# 표기: SO(3)/SE(3) 지수·로그, 좌·우 섭동, Plücker 선과 orthonormal (U,W) 표현,
#   핀홀 투영 + radial-tangential 왜곡, IMU preintegration.
#   6장(선)과 7장(IMU)이 앞 스터디들과 겹치므로 window.VM 을 그대로 물려받는다.

# ── 사이드바 머리말 (build_html.py) ──────────────────────────────────
brand     = Errors and Jacobians for SLAM
booktitle = Gyubeom Edward Im — 스터디 노트
